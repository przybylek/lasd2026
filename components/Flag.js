const Flag = ({ code }) => (
    <span className={`fib fi-${code} mr-1 mt-1`}
        style={{boxShadow: '0 0 1px #ddd, 0 0 1px #ddd, 0 0 1px #ddd', height:'19px', width:'25px'}}/>
)

export default Flag